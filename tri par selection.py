def swap (liste,i,j):
    temp=T[i]
    T[i]=T[j]
    T[j]=temp
T = [10,9,5,7,3]
for i in range (i,len(T)):
    if T[i] < j
    j=T[i]
    indice=i
    return 
        for j in range(j<len (T)): 
swap(T,i,j)
print(T)
